# EduMind AI — Frontend

React/Vite frontend for the EduMind AI Intelligent Academic Advisor.

## Run locally

```bash
npm install
npm run dev
```

The frontend is configured to communicate with the backend API on port 5000 during development.

See `../documentation/FRONTEND_SETUP.md` for the full setup and AI feature mapping.
